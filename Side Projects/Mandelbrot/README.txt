A set of mandelbrot renderers; one that does calculations on the CPU and one that does calculations on the GPU.

The primary purpose for this project was to learn more about mandelbrot set and to go through the process of converting from software render to hardware render.